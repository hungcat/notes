# はじめに

ここにメモを追加していきます。

`docs/` 以下に Markdown を置き、`docs/.vitepress/config.ts` の `sidebar` にリンクを足してください。

## ローカルプレビュー

```bash
npm install
npm run docs:dev
```

## ビルド

```bash
npm run docs:build
```
